; This is an INI file
[daemon]  ; daemon section

logfile = error.log								; to syslog

listen = 127.0.0.1:5555 						; lisen ip:port format [host:port]
daemon = 0
username = akalend

pidfile = /tmp/server.pid

ip2city_file = /home/akalend/projects/ngx_http_stat_module/db/net_city.txt	; 

[format]

// ; count of formats
count = 3

//;   the number is always first

number = 1
format = servername,remote_host,status,time,arg_server_id,cook_TestRoot,arg_pos,arg_user_id

number = 2
format = servername,host,arg_id

number = 3
format = servername,host,status,cook_TestRoot,arg_pos 

[end]